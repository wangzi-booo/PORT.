---
description: Reverse-engineer and clone one or more websites as pixel-perfect Next.js replicas. Use when the user asks to clone, replicate, rebuild, reverse-engineer, or copy a website.
argument-hint: "<url1> [<url2> ...]"
---

Read `${CLAUDE_PROJECT_DIR}/.agents/skills/clone-website/SKILL.md` completely, then follow it for the target URL or URLs in the user's request. When invoked directly, `$ARGUMENTS` contains those URL arguments.
